The reason I chose to use a generator 'clean_tickets' because it provides several reasons/benefits.It allows to process  the tickets one at a time, that can reduce memory footprint or we can say to improve memory efficiency because this is become handy if the list or input list will become large and used with loops and/or parallel processing context.If a malformed line is encountered , it simplifies error handling raises TicketFormatError with a message explaining the reason or why.Process flow could be like this :  generator catches it -> prints the reason ->  and skips on the next line, which nothing is yielded.In other words, generators are far more much valuable when it comes to scalable architecture as it does it improves memory efficiency and flexibility of the system,which this problem want to have and address.

In this solution or problem , the or a malformed line i assumed is one that doesnt match the expected format (like wrong number parts and/or invalid priority level)which this is handled by the custom exception called TicketFormatError