

# **PROCESS:**

First create all classes for Errors and Product. Create __init__ functions for each Classes, add sell function, __str__, ___repr__, & __eq__ for Product class. Added parameters to each functions. Then create Getter and Setter for Price and Quantity with functions inside @property returning the price and quantity. For setter add validator, as for the task the price should raise the Error if the value is 0 or negative, and for the quantity if value is negative, it will raise its own Error for quantity. Error classes stores the product name and value, the main class stores the parameters of the product, and since each has their own error like the price and quantity, they both have sub classes for error with unique error messages and these subclasse carries the product name and value. For the sell function, the n will be use to deduct the quantity of the product. If the value of n for sell is greater than the current value of quantity of the product, it will raise the InvalidQuantityError and also extending the reason. The function of str returns the format "Product name: $Price x Quantity in stock" and  the function for repr returns the format as "Product(name=Product name, price=Price, quantity=Quantity)". The function for eq compares both name, price, and quantity of the product. For outside the classes I created a build_inventory, rank_by_value functions, and copied the dataset. For the build Inventory function I created an empty lists for products and failures to seperate the two. Since the raw dataset has a bad data, this means I have to seperate or not include the failures. What counts as failures is what is base on the getters and setters where prices should not be 0 and negative and quantity should be not negative but can be 0. through those, it validates the product where if it passes it stores the product into the products list and fail stores to the failures. For the rank_by_value, using lambda sorted, returning the products by descending order. Lastly for the prints or the main block, created for loops for the products that passed and fail, where in fail with a custom message or reason for the skip. Also created a custom product to sell with values greater than the quantity of the product, which raises the InventoryError. Ranking the products by value using the function ranked_by_value, where it ranks the product by its total value price * quantity with an output decmial by 2.