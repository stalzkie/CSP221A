the Library Catalog is consist of 3 error handling exceptions and class named "Book" with a property and setter 
for year and copies. A constructor that is used to create and return a new instance of a class directly from a dictionary. 
This Python project handles two functions outside the Book class that focuses on handling data, "Build_Catalog" which focuses on securing and validating raw data and "rank_by_availability" that handles the sorting of valid books by their stock level(copies). while these two functions focus on handling upcoming data, there are 3 error hanlding classes that focus on ensuring that the upcoming data are valid and raised when they are not. they follow a Inheritance workflow having "CatalogError" as the main parent class and child classes named "Invalidyearerror" that handles year validity and "Invalidcopieserror" that focuses on the validity of the copies. for the "book" class, @year.setter checks if the year on a ceratin book is valid via checking if the book is greater than 2026 or less than the year 1450. @copies.setter on the other hand, validates upcoming data into the "book" safely isolating them for bad records with the use of custom exceptions (build_Catalog,rank_by_availability) the ranks them through their availability (copies). to ensure that the book has proper formatted outputs, this 3 user defined functions, __str__, __repr__, and __eq__. __str__ and __repr__ handles how book instances are converted to strings to more readable, while __eq__, uses direct comparisons between Books via the use of title, year,and copies. one minor user defined function  is "Checkout", is sole purpose is to subtract the number from a specific books stock when the user borrows them, and if they borrow more than the available copies of a book it raises and error "Invalidcopieserror"