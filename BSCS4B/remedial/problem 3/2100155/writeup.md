Averages and Flags stays vectorized because both methods operate on the entire array of robot at once, letting numPy's compiled internals process every row together instead of python stepping through them one at a time. It would be break if the moment you loop over the robots individually to compute each one's value, or check each cycle with ordinary conditional logic instead of an array-wide comparison.