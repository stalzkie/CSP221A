In this scenario, the weight rows with unparseable or missing data are dropped rather than replaced due to the fact that this pipeline seeks to check and log the actual weight of the delivery packages. Filling these values with an average misses the entire point of the pipeline, and might result to logistical failures. What we aim to do by dropping the said rows is to not only clean the data, but also provide actual, numerical data that can actually be validated.